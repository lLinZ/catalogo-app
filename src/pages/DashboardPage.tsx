import { FC } from 'react'
import { Layout } from '../components/layout'
import { Typography } from '@mui/material'

export const DashboardPage: FC = () => {
    return (
        <Layout title={'Home | Catalogo-app'}>

            <Typography>
                Hola vale
            </Typography>
        </Layout>
    )
}
