import { FC } from 'react'
import { Layout } from '../../components/layout'

export const EnterprisePage: FC = () => {
    return (
        <Layout title="Empresas | Catalogo-app">
            Enterprise
        </Layout>
    )
}